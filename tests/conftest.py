"""Pytest configuration and shared fixtures for apymix tests.

pytest-asyncio est en mode ``auto`` (configuré dans pyproject.toml),
donc tous les tests ``async def`` sont automatiquement marqués.
"""
